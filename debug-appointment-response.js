// Debug script to check appointment response format
console.log('Checking appointment endpoint response structure...');

// Wait for the next API call to see the actual response in logs
setTimeout(() => {
  console.log('Check the workflow console logs for the actual response structure');
  console.log('Look for: [express] GET /api/mcp/appointments 200');
}, 1000);